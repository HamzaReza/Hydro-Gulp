// The JS-facing wrapper lives in services/hydroBridge.ts and resolves the
// native module optionally (no-op in environments without the native build).
// This file exists so tooling treats the directory as a module root.
export {};
