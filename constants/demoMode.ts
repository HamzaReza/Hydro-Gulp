/**
 * Demo mode bakes a rich, deterministic dataset into Redux and disconnects
 * Firebase — used only to produce store screenshots. Enable by starting
 * Metro with EXPO_PUBLIC_DEMO_MODE=true; never set in production builds.
 */
export const DEMO_MODE = process.env.EXPO_PUBLIC_DEMO_MODE === "true";
